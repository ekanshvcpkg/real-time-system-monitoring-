1. Got some idea about the difference between CommonJS and ECMAScript modules. The main thing I understood is that CommonJS loads modules synchronously, while ECMAScript modules use asynchronous module loading, so ESM is generally the better option.

2. 